[tools]
neovim = "latest"
claude = "latest"
# kiro-cli installed via curl in devpod/setup until the mise registry entry is fixed
# (aqua:kiro.dev/kiro-cli currently lacks repo_owner/repo_name and fails to resolve)
# "kiro-cli" = "latest"
